Key Functionality: Collects key-value pairs from the map tasks and prepares them for shuffle.
Core Logic: The MapOutputCollector is responsible for collecting key-value pairs from the map tasks and preparing them for shuffle. It also provides a context object for the map tasks to report their status and progress.
Inputs/Outputs: The MapOutputCollector collects key-value pairs from the map tasks and prepares them for shuffle. It provides a context object for the map tasks to report their status and progress.
Dependencies: The MapOutputCollector depends on the MapTask, JobConf, and TaskReporter classes.