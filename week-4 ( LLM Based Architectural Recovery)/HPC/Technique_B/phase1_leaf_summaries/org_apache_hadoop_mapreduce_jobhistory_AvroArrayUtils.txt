Key Functionality:
- The code provides utility methods for working with Avro arrays in the context of MapReduce job history.

Core Logic:
- The code defines a schema for an array of integers and creates a null progress splits array using this schema.
- The toAvro method converts an array of integers to a List of Integers.
- The fromAvro method converts a List of Integers to an array of integers.

Inputs/Outputs:
- The toAvro method takes an array of integers as input and returns a List of Integers.
- The fromAvro method takes a List of Integers as input and returns an array of integers.

Dependencies: